#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

const ll mod = 10e+7;

void solve()
{
    int n;
    cin >> n;
    cout << (n / 10) + (n % 10) << '\n';
    return;
}
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    ll t;
    cin >> t;
    while (t--)
    {
        solve();
    }

    return 0;
}